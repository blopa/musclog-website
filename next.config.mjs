/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    // i18n: {
    //     locales: ['en-US', 'pt-BR', 'nl-NL'],
    //     defaultLocale: 'en-US',
    // },
};

export default nextConfig;
